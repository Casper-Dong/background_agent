# Slack App Manifest
# Import at: https://api.slack.com/apps → "Create New App" → "From a manifest"
# Replace YOUR_DOMAIN with your actual API endpoint.

display_information:
  name: Background Agent
  description: Run coding agent tasks from Slack
  background_color: "#4A154B"

features:
  bot_user:
    display_name: Background Agent
    always_online: true
  slash_commands:
    - command: /inspect
      url: https://YOUR_DOMAIN/api/slack/commands
      description: Start a background coding agent task
      usage_hint: "[task description]"
      should_escape: false

oauth_config:
  scopes:
    bot:
      - commands
      - chat:write
      - chat:write.public

settings:
  interactivity:
    is_enabled: true
    request_url: https://YOUR_DOMAIN/api/slack/interactions
  org_deploy_enabled: false
  socket_mode_enabled: false
  token_rotation_enabled: false
